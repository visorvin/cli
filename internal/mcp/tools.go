// Copyright 2026 visor. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/visorvin/cli/internal/cli"
	"github.com/visorvin/cli/internal/client"
	"github.com/visorvin/cli/internal/cliutil"
	"github.com/visorvin/cli/internal/config"
	"github.com/visorvin/cli/internal/mcp/cobratree"
	"github.com/visorvin/cli/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("dealers_get",
			mcplib.WithDescription("Returns one dealer summary by dealer_id. Missing dealers return 404 not_found_error. Required: dealer_id."),
			mcplib.WithString("dealer_id", mcplib.Required(), mcplib.Description("Dealer UUID.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/dealers/{dealer_id}", []mcpParamBinding{{PublicName: "dealer_id", WireName: "dealer_id", Location: "path"}}, []string{"dealer_id"}),
	)
	s.AddTool(
		mcplib.NewTool("dealers_list",
			mcplib.WithDescription("Returns dealer summaries ordered by active listing count. Use state and q to narrow results. Optional: limit, offset, state (plus 4 more)."),
			mcplib.WithString("limit", mcplib.Description("Page size as an integer string. Defaults to 50; maximum 100.")),
			mcplib.WithString("offset", mcplib.Description("Zero-based page offset as an integer string. Defaults to 0.")),
			mcplib.WithString("state", mcplib.Description("Comma-separated two-letter dealer states, for example CA,TX.")),
			mcplib.WithString("country", mcplib.Description("Dealer country code, for example US.")),
			mcplib.WithString("type", mcplib.Description("Dealer type.")),
			mcplib.WithString("make", mcplib.Description("Comma-separated represented franchise makes or slugs, for example toyota,honda.")),
			mcplib.WithString("q", mcplib.Description("Case-insensitive dealer name or website domain search string.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/dealers", []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "offset", WireName: "offset", Location: "query"}, {PublicName: "state", WireName: "state", Location: "query"}, {PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "type", WireName: "type", Location: "query"}, {PublicName: "make", WireName: "make", Location: "query"}, {PublicName: "q", WireName: "q", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("dealers_listings_dealers",
			mcplib.WithDescription("Returns listing summaries scoped to one dealer. Supports the listing filter and pagination surface. Required: dealer_id. Optional: limit, offset, sort (plus 60 more)."),
			mcplib.WithString("dealer_id", mcplib.Required(), mcplib.Description("Dealer UUID.")),
			mcplib.WithString("limit", mcplib.Description("Page size as an integer string. Defaults to 50; maximum 100.")),
			mcplib.WithString("offset", mcplib.Description("Zero-based page offset as an integer string. Defaults to 0.")),
			mcplib.WithString("sort", mcplib.Description("Sort order. Use field names with optional - for descending, for example price or -price. Defaults to days_on_market,...")),
			mcplib.WithString("make", mcplib.Description("Comma-separated make names or slugs, for example toyota,honda.")),
			mcplib.WithString("model", mcplib.Description("Comma-separated model names or slugs. Combine with make when possible for narrower results.")),
			mcplib.WithString("trim", mcplib.Description("Comma-separated trim names.")),
			mcplib.WithString("year", mcplib.Description("Comma-separated model years, for example 2023,2024.")),
			mcplib.WithString("state", mcplib.Description("Comma-separated two-letter dealer states, for example CA,TX.")),
			mcplib.WithString("dealer_type", mcplib.Description("Comma-separated dealer types, for example franchise,independent.")),
			mcplib.WithString("in_transit", mcplib.Description("Whether to return in-transit/build inventory or only stock inventory.")),
			mcplib.WithString("inventory_type", mcplib.Description("Comma-separated inventory classes such as new,used,certified.")),
			mcplib.WithString("body_type", mcplib.Description("Comma-separated body types.")),
			mcplib.WithString("transmission", mcplib.Description("Comma-separated transmission values.")),
			mcplib.WithString("drivetrain", mcplib.Description("Comma-separated drivetrain values.")),
			mcplib.WithString("assembly_location", mcplib.Description("Pipe-separated assembly locations. Uses | because locations often contain commas.")),
			mcplib.WithString("assembly_country", mcplib.Description("Comma-separated assembly country values.")),
			mcplib.WithString("fuel_type", mcplib.Description("Comma-separated fuel type values.")),
			mcplib.WithString("engine", mcplib.Description("Comma-separated engine descriptions.")),
			mcplib.WithString("version", mcplib.Description("Comma-separated vehicle version values.")),
			mcplib.WithString("exterior_color", mcplib.Description("Comma-separated exterior color values.")),
			mcplib.WithString("interior_color", mcplib.Description("Comma-separated interior color values.")),
			mcplib.WithString("base_exterior_color", mcplib.Description("Comma-separated normalized exterior color values.")),
			mcplib.WithString("base_interior_color", mcplib.Description("Comma-separated normalized interior color values.")),
			mcplib.WithString("seating_capacity", mcplib.Description("Comma-separated seating capacity integers.")),
			mcplib.WithString("cylinders", mcplib.Description("Comma-separated cylinder count integers.")),
			mcplib.WithString("doors", mcplib.Description("Comma-separated door count integers.")),
			mcplib.WithString("options_packages", mcplib.Description("Comma-separated manufacturer option/package codes.")),
			mcplib.WithString("features", mcplib.Description("Comma-separated feature tokens.")),
			mcplib.WithString("keywords", mcplib.Description("Comma-separated listing keyword tokens.")),
			mcplib.WithString("exclude_make", mcplib.Description("Comma-separated makes to exclude.")),
			mcplib.WithString("exclude_model", mcplib.Description("Comma-separated models to exclude.")),
			mcplib.WithString("exclude_trim", mcplib.Description("Comma-separated trims to exclude.")),
			mcplib.WithString("exclude_year", mcplib.Description("Comma-separated model years to exclude.")),
			mcplib.WithString("exclude_state", mcplib.Description("Comma-separated dealer states to exclude.")),
			mcplib.WithString("exclude_version", mcplib.Description("Comma-separated vehicle versions to exclude.")),
			mcplib.WithString("exclude_engine", mcplib.Description("Comma-separated engine descriptions to exclude.")),
			mcplib.WithString("exclude_assembly_location", mcplib.Description("Plus-separated assembly locations to exclude. Uses + to match the private API separator.")),
			mcplib.WithString("exclude_assembly_country", mcplib.Description("Comma-separated assembly countries to exclude.")),
			mcplib.WithString("exclude_exterior_color", mcplib.Description("Comma-separated exterior colors to exclude.")),
			mcplib.WithString("exclude_interior_color", mcplib.Description("Comma-separated interior colors to exclude.")),
			mcplib.WithString("exclude_base_exterior_color", mcplib.Description("Comma-separated normalized exterior colors to exclude.")),
			mcplib.WithString("exclude_base_interior_color", mcplib.Description("Comma-separated normalized interior colors to exclude.")),
			mcplib.WithString("exclude_options_packages", mcplib.Description("Comma-separated manufacturer option/package codes to exclude.")),
			mcplib.WithString("exclude_features", mcplib.Description("Comma-separated feature tokens to exclude.")),
			mcplib.WithString("exclude_fuel_type", mcplib.Description("Comma-separated fuel types to exclude.")),
			mcplib.WithString("exclude_powertrain_type", mcplib.Description("Comma-separated powertrain types to exclude.")),
			mcplib.WithString("exclude_keywords", mcplib.Description("Comma-separated keyword tokens to exclude.")),
			mcplib.WithString("inventory_status", mcplib.Description("Inventory mode. active is the default; sold searches historical sold inventory.")),
			mcplib.WithString("sold_within_days", mcplib.Description("Positive integer day window for sold inventory, for example 30. Cannot be combined with snapshot_date.")),
			mcplib.WithString("snapshot_date", mcplib.Description("Historical active-inventory snapshot date in YYYY-MM-DD format. Cannot be combined with sold inventory filters.")),
			mcplib.WithString("min_price", mcplib.Description("Minimum listed price in whole dollars.")),
			mcplib.WithString("max_price", mcplib.Description("Maximum listed price in whole dollars.")),
			mcplib.WithString("min_mileage", mcplib.Description("Minimum odometer mileage.")),
			mcplib.WithString("max_mileage", mcplib.Description("Maximum odometer mileage.")),
			mcplib.WithString("min_msrp", mcplib.Description("Minimum combined MSRP in whole dollars.")),
			mcplib.WithString("max_msrp", mcplib.Description("Maximum combined MSRP in whole dollars.")),
			mcplib.WithString("min_days_on_market", mcplib.Description("Minimum days on market.")),
			mcplib.WithString("max_days_on_market", mcplib.Description("Maximum days on market.")),
			mcplib.WithString("latitude", mcplib.Description("Search origin latitude for sort=distance, distance_miles, and radius filtering.")),
			mcplib.WithString("longitude", mcplib.Description("Search origin longitude for sort=distance, distance_miles, and radius filtering.")),
			mcplib.WithString("postal_code", mcplib.Description("US ZIP/postal search origin for sort=distance, distance_miles, and radius filtering.")),
			mcplib.WithString("radius", mcplib.Description("Maximum distance from the search origin, in miles.")),
			mcplib.WithString("bbox", mcplib.Description("Map viewport as west,south,east,north.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/dealers/{dealer_id}/listings", []mcpParamBinding{{PublicName: "dealer_id", WireName: "dealer_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "offset", WireName: "offset", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "make", WireName: "make", Location: "query"}, {PublicName: "model", WireName: "model", Location: "query"}, {PublicName: "trim", WireName: "trim", Location: "query"}, {PublicName: "year", WireName: "year", Location: "query"}, {PublicName: "state", WireName: "state", Location: "query"}, {PublicName: "dealer_type", WireName: "dealer_type", Location: "query"}, {PublicName: "in_transit", WireName: "in_transit", Location: "query"}, {PublicName: "inventory_type", WireName: "inventory_type", Location: "query"}, {PublicName: "body_type", WireName: "body_type", Location: "query"}, {PublicName: "transmission", WireName: "transmission", Location: "query"}, {PublicName: "drivetrain", WireName: "drivetrain", Location: "query"}, {PublicName: "assembly_location", WireName: "assembly_location", Location: "query"}, {PublicName: "assembly_country", WireName: "assembly_country", Location: "query"}, {PublicName: "fuel_type", WireName: "fuel_type", Location: "query"}, {PublicName: "engine", WireName: "engine", Location: "query"}, {PublicName: "version", WireName: "version", Location: "query"}, {PublicName: "exterior_color", WireName: "exterior_color", Location: "query"}, {PublicName: "interior_color", WireName: "interior_color", Location: "query"}, {PublicName: "base_exterior_color", WireName: "base_exterior_color", Location: "query"}, {PublicName: "base_interior_color", WireName: "base_interior_color", Location: "query"}, {PublicName: "seating_capacity", WireName: "seating_capacity", Location: "query"}, {PublicName: "cylinders", WireName: "cylinders", Location: "query"}, {PublicName: "doors", WireName: "doors", Location: "query"}, {PublicName: "options_packages", WireName: "options_packages", Location: "query"}, {PublicName: "features", WireName: "features", Location: "query"}, {PublicName: "keywords", WireName: "keywords", Location: "query"}, {PublicName: "exclude_make", WireName: "exclude_make", Location: "query"}, {PublicName: "exclude_model", WireName: "exclude_model", Location: "query"}, {PublicName: "exclude_trim", WireName: "exclude_trim", Location: "query"}, {PublicName: "exclude_year", WireName: "exclude_year", Location: "query"}, {PublicName: "exclude_state", WireName: "exclude_state", Location: "query"}, {PublicName: "exclude_version", WireName: "exclude_version", Location: "query"}, {PublicName: "exclude_engine", WireName: "exclude_engine", Location: "query"}, {PublicName: "exclude_assembly_location", WireName: "exclude_assembly_location", Location: "query"}, {PublicName: "exclude_assembly_country", WireName: "exclude_assembly_country", Location: "query"}, {PublicName: "exclude_exterior_color", WireName: "exclude_exterior_color", Location: "query"}, {PublicName: "exclude_interior_color", WireName: "exclude_interior_color", Location: "query"}, {PublicName: "exclude_base_exterior_color", WireName: "exclude_base_exterior_color", Location: "query"}, {PublicName: "exclude_base_interior_color", WireName: "exclude_base_interior_color", Location: "query"}, {PublicName: "exclude_options_packages", WireName: "exclude_options_packages", Location: "query"}, {PublicName: "exclude_features", WireName: "exclude_features", Location: "query"}, {PublicName: "exclude_fuel_type", WireName: "exclude_fuel_type", Location: "query"}, {PublicName: "exclude_powertrain_type", WireName: "exclude_powertrain_type", Location: "query"}, {PublicName: "exclude_keywords", WireName: "exclude_keywords", Location: "query"}, {PublicName: "inventory_status", WireName: "inventory_status", Location: "query"}, {PublicName: "sold_within_days", WireName: "sold_within_days", Location: "query"}, {PublicName: "snapshot_date", WireName: "snapshot_date", Location: "query"}, {PublicName: "min_price", WireName: "min_price", Location: "query"}, {PublicName: "max_price", WireName: "max_price", Location: "query"}, {PublicName: "min_mileage", WireName: "min_mileage", Location: "query"}, {PublicName: "max_mileage", WireName: "max_mileage", Location: "query"}, {PublicName: "min_msrp", WireName: "min_msrp", Location: "query"}, {PublicName: "max_msrp", WireName: "max_msrp", Location: "query"}, {PublicName: "min_days_on_market", WireName: "min_days_on_market", Location: "query"}, {PublicName: "max_days_on_market", WireName: "max_days_on_market", Location: "query"}, {PublicName: "latitude", WireName: "latitude", Location: "query"}, {PublicName: "longitude", WireName: "longitude", Location: "query"}, {PublicName: "postal_code", WireName: "postal_code", Location: "query"}, {PublicName: "radius", WireName: "radius", Location: "query"}, {PublicName: "bbox", WireName: "bbox", Location: "query"}}, []string{"dealer_id"}),
	)
	s.AddTool(
		mcplib.NewTool("facets_list",
			mcplib.WithDescription("Returns categorical facets, numeric range facets, and stats for the listing filter surface. Use facets=make,model,price to return a subset; omit facets for all supported facets. Optional: facets, make, model (plus 58 more)."),
			mcplib.WithString("facets", mcplib.Description("Comma-separated facet names to return. Defaults to all supported facets.")),
			mcplib.WithString("make", mcplib.Description("Comma-separated make names or slugs to apply before counting facet buckets.")),
			mcplib.WithString("model", mcplib.Description("Comma-separated model names or slugs to apply before counting facet buckets.")),
			mcplib.WithString("trim", mcplib.Description("Comma-separated trim names to apply before counting facet buckets.")),
			mcplib.WithString("year", mcplib.Description("Comma-separated model years to apply before counting facet buckets.")),
			mcplib.WithString("state", mcplib.Description("Comma-separated two-letter dealer states to apply before counting facet buckets.")),
			mcplib.WithString("dealer_type", mcplib.Description("Comma-separated dealer types to apply before counting facet buckets.")),
			mcplib.WithString("in_transit", mcplib.Description("Whether to count facets for in-transit/build inventory or stock inventory.")),
			mcplib.WithString("inventory_type", mcplib.Description("Comma-separated inventory classes such as new,used,certified.")),
			mcplib.WithString("body_type", mcplib.Description("Comma-separated body types.")),
			mcplib.WithString("transmission", mcplib.Description("Comma-separated transmission values.")),
			mcplib.WithString("drivetrain", mcplib.Description("Comma-separated drivetrain values.")),
			mcplib.WithString("assembly_location", mcplib.Description("Pipe-separated assembly locations. Uses | because locations often contain commas.")),
			mcplib.WithString("assembly_country", mcplib.Description("Comma-separated assembly country values.")),
			mcplib.WithString("fuel_type", mcplib.Description("Comma-separated fuel type values.")),
			mcplib.WithString("engine", mcplib.Description("Comma-separated engine descriptions.")),
			mcplib.WithString("version", mcplib.Description("Comma-separated vehicle version values.")),
			mcplib.WithString("exterior_color", mcplib.Description("Comma-separated exterior color values.")),
			mcplib.WithString("interior_color", mcplib.Description("Comma-separated interior color values.")),
			mcplib.WithString("base_exterior_color", mcplib.Description("Comma-separated normalized exterior color values.")),
			mcplib.WithString("base_interior_color", mcplib.Description("Comma-separated normalized interior color values.")),
			mcplib.WithString("seating_capacity", mcplib.Description("Comma-separated seating capacity integers.")),
			mcplib.WithString("cylinders", mcplib.Description("Comma-separated cylinder count integers.")),
			mcplib.WithString("doors", mcplib.Description("Comma-separated door count integers.")),
			mcplib.WithString("options_packages", mcplib.Description("Comma-separated manufacturer option/package codes.")),
			mcplib.WithString("features", mcplib.Description("Comma-separated feature tokens.")),
			mcplib.WithString("keywords", mcplib.Description("Comma-separated listing keyword tokens.")),
			mcplib.WithString("exclude_make", mcplib.Description("Comma-separated makes to exclude.")),
			mcplib.WithString("exclude_model", mcplib.Description("Comma-separated models to exclude.")),
			mcplib.WithString("exclude_trim", mcplib.Description("Comma-separated trims to exclude.")),
			mcplib.WithString("exclude_year", mcplib.Description("Comma-separated model years to exclude.")),
			mcplib.WithString("exclude_state", mcplib.Description("Comma-separated dealer states to exclude.")),
			mcplib.WithString("exclude_version", mcplib.Description("Comma-separated vehicle versions to exclude.")),
			mcplib.WithString("exclude_engine", mcplib.Description("Comma-separated engine descriptions to exclude.")),
			mcplib.WithString("exclude_assembly_location", mcplib.Description("Plus-separated assembly locations to exclude. Uses + to match the private API separator.")),
			mcplib.WithString("exclude_assembly_country", mcplib.Description("Comma-separated assembly countries to exclude.")),
			mcplib.WithString("exclude_exterior_color", mcplib.Description("Comma-separated exterior colors to exclude.")),
			mcplib.WithString("exclude_interior_color", mcplib.Description("Comma-separated interior colors to exclude.")),
			mcplib.WithString("exclude_base_exterior_color", mcplib.Description("Comma-separated normalized exterior colors to exclude.")),
			mcplib.WithString("exclude_base_interior_color", mcplib.Description("Comma-separated normalized interior colors to exclude.")),
			mcplib.WithString("exclude_options_packages", mcplib.Description("Comma-separated manufacturer option/package codes to exclude.")),
			mcplib.WithString("exclude_features", mcplib.Description("Comma-separated feature tokens to exclude.")),
			mcplib.WithString("exclude_fuel_type", mcplib.Description("Comma-separated fuel types to exclude.")),
			mcplib.WithString("exclude_powertrain_type", mcplib.Description("Comma-separated powertrain types to exclude.")),
			mcplib.WithString("exclude_keywords", mcplib.Description("Comma-separated keyword tokens to exclude.")),
			mcplib.WithString("inventory_status", mcplib.Description("Inventory mode used for facet counts. active is the default.")),
			mcplib.WithString("sold_within_days", mcplib.Description("Positive integer day window for sold inventory facets.")),
			mcplib.WithString("snapshot_date", mcplib.Description("Historical active-inventory snapshot date in YYYY-MM-DD format.")),
			mcplib.WithString("min_price", mcplib.Description("Minimum listed price in whole dollars.")),
			mcplib.WithString("max_price", mcplib.Description("Maximum listed price in whole dollars.")),
			mcplib.WithString("min_mileage", mcplib.Description("Minimum odometer mileage.")),
			mcplib.WithString("max_mileage", mcplib.Description("Maximum odometer mileage.")),
			mcplib.WithString("min_msrp", mcplib.Description("Minimum combined MSRP in whole dollars.")),
			mcplib.WithString("max_msrp", mcplib.Description("Maximum combined MSRP in whole dollars.")),
			mcplib.WithString("min_days_on_market", mcplib.Description("Minimum days on market.")),
			mcplib.WithString("max_days_on_market", mcplib.Description("Maximum days on market.")),
			mcplib.WithString("latitude", mcplib.Description("Search origin latitude for radius filtering.")),
			mcplib.WithString("longitude", mcplib.Description("Search origin longitude for radius filtering.")),
			mcplib.WithString("postal_code", mcplib.Description("US ZIP/postal search origin for radius filtering.")),
			mcplib.WithString("radius", mcplib.Description("Maximum distance from the search origin, in miles.")),
			mcplib.WithString("bbox", mcplib.Description("Map viewport as west,south,east,north.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/facets", []mcpParamBinding{{PublicName: "facets", WireName: "facets", Location: "query"}, {PublicName: "make", WireName: "make", Location: "query"}, {PublicName: "model", WireName: "model", Location: "query"}, {PublicName: "trim", WireName: "trim", Location: "query"}, {PublicName: "year", WireName: "year", Location: "query"}, {PublicName: "state", WireName: "state", Location: "query"}, {PublicName: "dealer_type", WireName: "dealer_type", Location: "query"}, {PublicName: "in_transit", WireName: "in_transit", Location: "query"}, {PublicName: "inventory_type", WireName: "inventory_type", Location: "query"}, {PublicName: "body_type", WireName: "body_type", Location: "query"}, {PublicName: "transmission", WireName: "transmission", Location: "query"}, {PublicName: "drivetrain", WireName: "drivetrain", Location: "query"}, {PublicName: "assembly_location", WireName: "assembly_location", Location: "query"}, {PublicName: "assembly_country", WireName: "assembly_country", Location: "query"}, {PublicName: "fuel_type", WireName: "fuel_type", Location: "query"}, {PublicName: "engine", WireName: "engine", Location: "query"}, {PublicName: "version", WireName: "version", Location: "query"}, {PublicName: "exterior_color", WireName: "exterior_color", Location: "query"}, {PublicName: "interior_color", WireName: "interior_color", Location: "query"}, {PublicName: "base_exterior_color", WireName: "base_exterior_color", Location: "query"}, {PublicName: "base_interior_color", WireName: "base_interior_color", Location: "query"}, {PublicName: "seating_capacity", WireName: "seating_capacity", Location: "query"}, {PublicName: "cylinders", WireName: "cylinders", Location: "query"}, {PublicName: "doors", WireName: "doors", Location: "query"}, {PublicName: "options_packages", WireName: "options_packages", Location: "query"}, {PublicName: "features", WireName: "features", Location: "query"}, {PublicName: "keywords", WireName: "keywords", Location: "query"}, {PublicName: "exclude_make", WireName: "exclude_make", Location: "query"}, {PublicName: "exclude_model", WireName: "exclude_model", Location: "query"}, {PublicName: "exclude_trim", WireName: "exclude_trim", Location: "query"}, {PublicName: "exclude_year", WireName: "exclude_year", Location: "query"}, {PublicName: "exclude_state", WireName: "exclude_state", Location: "query"}, {PublicName: "exclude_version", WireName: "exclude_version", Location: "query"}, {PublicName: "exclude_engine", WireName: "exclude_engine", Location: "query"}, {PublicName: "exclude_assembly_location", WireName: "exclude_assembly_location", Location: "query"}, {PublicName: "exclude_assembly_country", WireName: "exclude_assembly_country", Location: "query"}, {PublicName: "exclude_exterior_color", WireName: "exclude_exterior_color", Location: "query"}, {PublicName: "exclude_interior_color", WireName: "exclude_interior_color", Location: "query"}, {PublicName: "exclude_base_exterior_color", WireName: "exclude_base_exterior_color", Location: "query"}, {PublicName: "exclude_base_interior_color", WireName: "exclude_base_interior_color", Location: "query"}, {PublicName: "exclude_options_packages", WireName: "exclude_options_packages", Location: "query"}, {PublicName: "exclude_features", WireName: "exclude_features", Location: "query"}, {PublicName: "exclude_fuel_type", WireName: "exclude_fuel_type", Location: "query"}, {PublicName: "exclude_powertrain_type", WireName: "exclude_powertrain_type", Location: "query"}, {PublicName: "exclude_keywords", WireName: "exclude_keywords", Location: "query"}, {PublicName: "inventory_status", WireName: "inventory_status", Location: "query"}, {PublicName: "sold_within_days", WireName: "sold_within_days", Location: "query"}, {PublicName: "snapshot_date", WireName: "snapshot_date", Location: "query"}, {PublicName: "min_price", WireName: "min_price", Location: "query"}, {PublicName: "max_price", WireName: "max_price", Location: "query"}, {PublicName: "min_mileage", WireName: "min_mileage", Location: "query"}, {PublicName: "max_mileage", WireName: "max_mileage", Location: "query"}, {PublicName: "min_msrp", WireName: "min_msrp", Location: "query"}, {PublicName: "max_msrp", WireName: "max_msrp", Location: "query"}, {PublicName: "min_days_on_market", WireName: "min_days_on_market", Location: "query"}, {PublicName: "max_days_on_market", WireName: "max_days_on_market", Location: "query"}, {PublicName: "latitude", WireName: "latitude", Location: "query"}, {PublicName: "longitude", WireName: "longitude", Location: "query"}, {PublicName: "postal_code", WireName: "postal_code", Location: "query"}, {PublicName: "radius", WireName: "radius", Location: "query"}, {PublicName: "bbox", WireName: "bbox", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("listings_get",
			mcplib.WithDescription("Returns listing-centered detail by listing_id. Missing listings return 404 not_found_error. Required: listing_id. Optional: include."),
			mcplib.WithString("listing_id", mcplib.Required(), mcplib.Description("Stable listing identifier.")),
			mcplib.WithString("include", mcplib.Description("Comma-separated optional expansions. Supported values are price_history and options.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/listings/{listing_id}", []mcpParamBinding{{PublicName: "listing_id", WireName: "listing_id", Location: "path"}, {PublicName: "include", WireName: "include", Location: "query"}}, []string{"listing_id"}),
	)
	s.AddTool(
		mcplib.NewTool("listings_list",
			mcplib.WithDescription("Returns active, sold, or historical snapshot listing summaries using stable snake_case fields. Unknown filters fail closed with a validation_error. Optional: limit, offset, sort (plus 60 more)."),
			mcplib.WithString("limit", mcplib.Description("Page size as an integer string. Defaults to 50; maximum 100.")),
			mcplib.WithString("offset", mcplib.Description("Zero-based page offset as an integer string. Defaults to 0.")),
			mcplib.WithString("sort", mcplib.Description("Sort order. Use field names with optional - for descending, for example price or -price. Defaults to days_on_market,...")),
			mcplib.WithString("make", mcplib.Description("Comma-separated make names or slugs, for example toyota,honda.")),
			mcplib.WithString("model", mcplib.Description("Comma-separated model names or slugs. Combine with make when possible for narrower results.")),
			mcplib.WithString("trim", mcplib.Description("Comma-separated trim names.")),
			mcplib.WithString("year", mcplib.Description("Comma-separated model years, for example 2023,2024.")),
			mcplib.WithString("state", mcplib.Description("Comma-separated two-letter dealer states, for example CA,TX.")),
			mcplib.WithString("dealer_type", mcplib.Description("Comma-separated dealer types, for example franchise,independent.")),
			mcplib.WithString("in_transit", mcplib.Description("Whether to return in-transit/build inventory or only stock inventory.")),
			mcplib.WithString("inventory_type", mcplib.Description("Comma-separated inventory classes such as new,used,certified.")),
			mcplib.WithString("body_type", mcplib.Description("Comma-separated body types.")),
			mcplib.WithString("transmission", mcplib.Description("Comma-separated transmission values.")),
			mcplib.WithString("drivetrain", mcplib.Description("Comma-separated drivetrain values.")),
			mcplib.WithString("assembly_location", mcplib.Description("Pipe-separated assembly locations. Uses | because locations often contain commas.")),
			mcplib.WithString("assembly_country", mcplib.Description("Comma-separated assembly country values.")),
			mcplib.WithString("fuel_type", mcplib.Description("Comma-separated fuel type values.")),
			mcplib.WithString("engine", mcplib.Description("Comma-separated engine descriptions.")),
			mcplib.WithString("version", mcplib.Description("Comma-separated vehicle version values.")),
			mcplib.WithString("exterior_color", mcplib.Description("Comma-separated exterior color values.")),
			mcplib.WithString("interior_color", mcplib.Description("Comma-separated interior color values.")),
			mcplib.WithString("base_exterior_color", mcplib.Description("Comma-separated normalized exterior color values.")),
			mcplib.WithString("base_interior_color", mcplib.Description("Comma-separated normalized interior color values.")),
			mcplib.WithString("seating_capacity", mcplib.Description("Comma-separated seating capacity integers.")),
			mcplib.WithString("cylinders", mcplib.Description("Comma-separated cylinder count integers.")),
			mcplib.WithString("doors", mcplib.Description("Comma-separated door count integers.")),
			mcplib.WithString("options_packages", mcplib.Description("Comma-separated manufacturer option/package codes.")),
			mcplib.WithString("features", mcplib.Description("Comma-separated feature tokens.")),
			mcplib.WithString("keywords", mcplib.Description("Comma-separated listing keyword tokens.")),
			mcplib.WithString("exclude_make", mcplib.Description("Comma-separated makes to exclude.")),
			mcplib.WithString("exclude_model", mcplib.Description("Comma-separated models to exclude.")),
			mcplib.WithString("exclude_trim", mcplib.Description("Comma-separated trims to exclude.")),
			mcplib.WithString("exclude_year", mcplib.Description("Comma-separated model years to exclude.")),
			mcplib.WithString("exclude_state", mcplib.Description("Comma-separated dealer states to exclude.")),
			mcplib.WithString("exclude_version", mcplib.Description("Comma-separated vehicle versions to exclude.")),
			mcplib.WithString("exclude_engine", mcplib.Description("Comma-separated engine descriptions to exclude.")),
			mcplib.WithString("exclude_assembly_location", mcplib.Description("Plus-separated assembly locations to exclude. Uses + to match the private API separator.")),
			mcplib.WithString("exclude_assembly_country", mcplib.Description("Comma-separated assembly countries to exclude.")),
			mcplib.WithString("exclude_exterior_color", mcplib.Description("Comma-separated exterior colors to exclude.")),
			mcplib.WithString("exclude_interior_color", mcplib.Description("Comma-separated interior colors to exclude.")),
			mcplib.WithString("exclude_base_exterior_color", mcplib.Description("Comma-separated normalized exterior colors to exclude.")),
			mcplib.WithString("exclude_base_interior_color", mcplib.Description("Comma-separated normalized interior colors to exclude.")),
			mcplib.WithString("exclude_options_packages", mcplib.Description("Comma-separated manufacturer option/package codes to exclude.")),
			mcplib.WithString("exclude_features", mcplib.Description("Comma-separated feature tokens to exclude.")),
			mcplib.WithString("exclude_fuel_type", mcplib.Description("Comma-separated fuel types to exclude.")),
			mcplib.WithString("exclude_powertrain_type", mcplib.Description("Comma-separated powertrain types to exclude.")),
			mcplib.WithString("exclude_keywords", mcplib.Description("Comma-separated keyword tokens to exclude.")),
			mcplib.WithString("inventory_status", mcplib.Description("Inventory mode. active is the default; sold searches historical sold inventory.")),
			mcplib.WithString("sold_within_days", mcplib.Description("Positive integer day window for sold inventory, for example 30. Cannot be combined with snapshot_date.")),
			mcplib.WithString("snapshot_date", mcplib.Description("Historical active-inventory snapshot date in YYYY-MM-DD format. Cannot be combined with sold inventory filters.")),
			mcplib.WithString("min_price", mcplib.Description("Minimum listed price in whole dollars.")),
			mcplib.WithString("max_price", mcplib.Description("Maximum listed price in whole dollars.")),
			mcplib.WithString("min_mileage", mcplib.Description("Minimum odometer mileage.")),
			mcplib.WithString("max_mileage", mcplib.Description("Maximum odometer mileage.")),
			mcplib.WithString("min_msrp", mcplib.Description("Minimum combined MSRP in whole dollars.")),
			mcplib.WithString("max_msrp", mcplib.Description("Maximum combined MSRP in whole dollars.")),
			mcplib.WithString("min_days_on_market", mcplib.Description("Minimum days on market.")),
			mcplib.WithString("max_days_on_market", mcplib.Description("Maximum days on market.")),
			mcplib.WithString("latitude", mcplib.Description("Search origin latitude for sort=distance, distance_miles, and radius filtering.")),
			mcplib.WithString("longitude", mcplib.Description("Search origin longitude for sort=distance, distance_miles, and radius filtering.")),
			mcplib.WithString("postal_code", mcplib.Description("US ZIP/postal search origin for sort=distance, distance_miles, and radius filtering.")),
			mcplib.WithString("radius", mcplib.Description("Maximum distance from the search origin, in miles.")),
			mcplib.WithString("bbox", mcplib.Description("Map viewport as west,south,east,north.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/listings", []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "offset", WireName: "offset", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "make", WireName: "make", Location: "query"}, {PublicName: "model", WireName: "model", Location: "query"}, {PublicName: "trim", WireName: "trim", Location: "query"}, {PublicName: "year", WireName: "year", Location: "query"}, {PublicName: "state", WireName: "state", Location: "query"}, {PublicName: "dealer_type", WireName: "dealer_type", Location: "query"}, {PublicName: "in_transit", WireName: "in_transit", Location: "query"}, {PublicName: "inventory_type", WireName: "inventory_type", Location: "query"}, {PublicName: "body_type", WireName: "body_type", Location: "query"}, {PublicName: "transmission", WireName: "transmission", Location: "query"}, {PublicName: "drivetrain", WireName: "drivetrain", Location: "query"}, {PublicName: "assembly_location", WireName: "assembly_location", Location: "query"}, {PublicName: "assembly_country", WireName: "assembly_country", Location: "query"}, {PublicName: "fuel_type", WireName: "fuel_type", Location: "query"}, {PublicName: "engine", WireName: "engine", Location: "query"}, {PublicName: "version", WireName: "version", Location: "query"}, {PublicName: "exterior_color", WireName: "exterior_color", Location: "query"}, {PublicName: "interior_color", WireName: "interior_color", Location: "query"}, {PublicName: "base_exterior_color", WireName: "base_exterior_color", Location: "query"}, {PublicName: "base_interior_color", WireName: "base_interior_color", Location: "query"}, {PublicName: "seating_capacity", WireName: "seating_capacity", Location: "query"}, {PublicName: "cylinders", WireName: "cylinders", Location: "query"}, {PublicName: "doors", WireName: "doors", Location: "query"}, {PublicName: "options_packages", WireName: "options_packages", Location: "query"}, {PublicName: "features", WireName: "features", Location: "query"}, {PublicName: "keywords", WireName: "keywords", Location: "query"}, {PublicName: "exclude_make", WireName: "exclude_make", Location: "query"}, {PublicName: "exclude_model", WireName: "exclude_model", Location: "query"}, {PublicName: "exclude_trim", WireName: "exclude_trim", Location: "query"}, {PublicName: "exclude_year", WireName: "exclude_year", Location: "query"}, {PublicName: "exclude_state", WireName: "exclude_state", Location: "query"}, {PublicName: "exclude_version", WireName: "exclude_version", Location: "query"}, {PublicName: "exclude_engine", WireName: "exclude_engine", Location: "query"}, {PublicName: "exclude_assembly_location", WireName: "exclude_assembly_location", Location: "query"}, {PublicName: "exclude_assembly_country", WireName: "exclude_assembly_country", Location: "query"}, {PublicName: "exclude_exterior_color", WireName: "exclude_exterior_color", Location: "query"}, {PublicName: "exclude_interior_color", WireName: "exclude_interior_color", Location: "query"}, {PublicName: "exclude_base_exterior_color", WireName: "exclude_base_exterior_color", Location: "query"}, {PublicName: "exclude_base_interior_color", WireName: "exclude_base_interior_color", Location: "query"}, {PublicName: "exclude_options_packages", WireName: "exclude_options_packages", Location: "query"}, {PublicName: "exclude_features", WireName: "exclude_features", Location: "query"}, {PublicName: "exclude_fuel_type", WireName: "exclude_fuel_type", Location: "query"}, {PublicName: "exclude_powertrain_type", WireName: "exclude_powertrain_type", Location: "query"}, {PublicName: "exclude_keywords", WireName: "exclude_keywords", Location: "query"}, {PublicName: "inventory_status", WireName: "inventory_status", Location: "query"}, {PublicName: "sold_within_days", WireName: "sold_within_days", Location: "query"}, {PublicName: "snapshot_date", WireName: "snapshot_date", Location: "query"}, {PublicName: "min_price", WireName: "min_price", Location: "query"}, {PublicName: "max_price", WireName: "max_price", Location: "query"}, {PublicName: "min_mileage", WireName: "min_mileage", Location: "query"}, {PublicName: "max_mileage", WireName: "max_mileage", Location: "query"}, {PublicName: "min_msrp", WireName: "min_msrp", Location: "query"}, {PublicName: "max_msrp", WireName: "max_msrp", Location: "query"}, {PublicName: "min_days_on_market", WireName: "min_days_on_market", Location: "query"}, {PublicName: "max_days_on_market", WireName: "max_days_on_market", Location: "query"}, {PublicName: "latitude", WireName: "latitude", Location: "query"}, {PublicName: "longitude", WireName: "longitude", Location: "query"}, {PublicName: "postal_code", WireName: "postal_code", Location: "query"}, {PublicName: "radius", WireName: "radius", Location: "query"}, {PublicName: "bbox", WireName: "bbox", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("vins_get",
			mcplib.WithDescription("Returns the current or latest known record for a VIN. Missing VINs return 404 not_found_error. Required: vin. Optional: include."),
			mcplib.WithString("vin", mcplib.Required(), mcplib.Description("17-character VIN.")),
			mcplib.WithString("include", mcplib.Description("Comma-separated optional expansions. Supported values are price_history and options.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/vins/{vin}", []mcpParamBinding{{PublicName: "vin", WireName: "vin", Location: "path"}, {PublicName: "include", WireName: "include", Location: "query"}}, []string{"vin"}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Post(path, body)
		case "PUT":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Put(path, body)
		case "PATCH":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Patch(path, body)
		case "DELETE":
			data, _, err = c.Delete(path)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export VISOR_API_KEY=<your-key>" +
					"\n      Run 'visor doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your token." +
					"\n      Set it with: export VISOR_API_KEY=<your-key>" +
					"\n      Run 'visor doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export VISOR_API_KEY=<your-key>" +
					"\n      Run 'visor doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "visor", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "visor", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "visor",
		"description": "Query Visor vehicle listings, facets, VIN detail, and dealer inventory from a scriptable CLI.",
		"archetype":   "crm",
		"tool_count":  7,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion visor binary.",
		"auth": map[string]any{
			"type": "bearer_token",
			"env_vars": []map[string]any{
				{
					"name":        "VISOR_API_KEY",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
		},
		"resources": []map[string]any{
			{
				"name":        "dealers",
				"description": "Manage dealers",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "facets",
				"description": "Manage facets",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "listings",
				"description": "Manage listings",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "vins",
				"description": "Manage vins",
				"endpoints":   []string{"get"},
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass offset parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		"playbook": []map[string]string{
			{"topic": "Contact lookup", "insight": "Use search for finding contacts by name/email. List endpoints return unsorted results and require pagination for large datasets."},
			{"topic": "Activity tracking", "insight": "When checking deal activity, sync first and query locally. CRM APIs often throttle activity-log endpoints heavily."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
